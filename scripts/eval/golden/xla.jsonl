{"id": "xla-01", "repo": "xla", "question": "How does an out-of-tree accelerator backend (say Intel oneAPI or AWS Trainium) plug into XLA so that frameworks like JAX discover it at runtime? Name the single symbol such a plugin must export, the struct it returns, and how the framework loads it.", "expect_all": [["GetPjrtApi"], ["PJRT_Api"], ["dlopen", "\\.so", "shared library", "function pointers"]]}
{"id": "xla-02", "repo": "xla", "question": "In XLA's GPU backend, what single dispatcher function chooses the codegen path for each fusion, and which backends can it map a fusion to? Is cuBLAS GEMM one of those fusion emitters — if not, how do dot ops reach cuBLAS?", "expect_all": [["GetFusionEmitter"], ["Triton"], ["GemmRewriter", "custom.call", "__cublas"]]}
{"id": "xla-03", "repo": "xla", "question": "In openxla/xla I'm hunting for the class that orchestrates the whole GPU compilation pipeline (the one with RunHloPasses / RunBackend overrides). Should I look under xla/backends/gpu/? Where does it actually live, and what does xla/backends/ hold instead?", "expect_all": [["GpuCompiler"], ["xla/service", "service/gpu"], ["emitter", "codegen", "thunk", "runtime"]]}
{"id": "xla-04", "repo": "xla", "question": "What execution artifact does XLA's GPU compiler emit for the runtime to replay on each execution, what serialization constraint must it obey and what does that constraint enable, and where is its base class defined?", "expect_all": [["thunk"], ["serializ"], ["back-pointer", "HloInstruction", "StreamExecutor", "AOT"]]}
