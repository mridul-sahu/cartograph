---
layer: seams
last_revised: 1970-01-01
---

# Cross-repo seams

No seams yet. As you discover cross-repo relationships (e.g.
"repo-A's `foo()` calls into repo-B's `bar()`"), append entries here
via the `/seam` slash command, or edit this file directly.

Format per entry:

```markdown
## <short title> — <repo-a> ↔ <repo-b>

(One paragraph describing the relationship. Cite `path:NNN` anchors
in both repos.)
```
